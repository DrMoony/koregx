# KoRegX v0.1 Slice 1 — 식약처 Vertical Slice Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 식약처 의약품 검색 + 상세 dossier query를 Web UI + MCP server 양쪽에서 동작시키는 first end-to-end vertical slice. Acceptance criterion: "Wegovy 한국 허가 timeline" query가 자연어로 작동.

**Architecture:** Next.js 16 App Router scaffold + Prisma 7 ORM + Postgres(Neon) + 식약처 OpenAPI client + MCP server endpoint (`/api/mcp`). Source plugin abstract base를 둬서 후속 slice (HIRA, FDA, EMA, PMDA, NMPA)에서 같은 패턴 재사용. pgvector extension만 활성화하고 임베딩 파이프라인은 Slice 4에서 본격화.

**Tech Stack:** Next.js 16, React 19, Tailwind CSS 4, shadcn/ui, Prisma 7 driver adapter, Postgres + pgvector (Neon), Clerk auth, Gemini 2.5 Flash, @modelcontextprotocol/sdk (TypeScript), Vitest, Playwright

---

## Scope

**In scope (Slice 1)**:
- M0: 프로젝트 scaffold (Next.js + Prisma + Clerk + Tailwind + shadcn)
- M1: DB schema first migration (Drug, Approval, SourceDocument)
- M2: Source plugin abstract base
- M3: 식약처 OpenAPI client + ingest service
- M4: Web UI — search + drug detail page
- M5: MCP server — 3 tools (search_drug, get_drug, chain_drug_dossier)
- M6: 도메인 가용성 확인 + Vercel + Neon 연결 + first deploy
- M7: Claude Desktop connector instruction
- M8: Acceptance test — "Wegovy 한국 허가 timeline" 자연어 query 작동

**Out of scope (defer to future slices)**:
- HIRA 약평위/암질심 (Slice 2)
- 복지부 고시 (Slice 3)
- pgvector embedding pipeline + LLM Q&A orchestrator (Slice 4)
- KPIS + cron hardening + deploy production-grade (Slice 5)
- Cross-region (v0.5+)
- 의료기기 (v2)

---

## File Structure

```
koregx/
├── .env.example                          # 환경변수 템플릿
├── .gitignore                            (existing)
├── README.md                             (existing, update at M6)
├── package.json                          (M0)
├── tsconfig.json                         (M0)
├── next.config.ts                        (M0)
├── postcss.config.mjs                    (M0)
├── eslint.config.mjs                     (M0)
├── components.json                       # shadcn config (M0)
├── prisma/
│   ├── schema.prisma                     (M1)
│   └── migrations/                       (M1)
├── src/
│   ├── middleware.ts                     # Clerk route protection (M0)
│   ├── app/
│   │   ├── layout.tsx                    (M0)
│   │   ├── page.tsx                      # Landing (M0)
│   │   ├── globals.css                   # Tailwind + design tokens (M0)
│   │   ├── search/
│   │   │   └── page.tsx                  (M4)
│   │   ├── drug/
│   │   │   └── [id]/page.tsx             (M4)
│   │   └── api/
│   │       └── mcp/
│   │           └── route.ts              # MCP server endpoint (M5)
│   ├── lib/
│   │   ├── db.ts                         # Prisma client w/ driver adapter (M1)
│   │   ├── sources/
│   │   │   ├── base.ts                   # SourcePlugin abstract (M2)
│   │   │   ├── mfds.ts                   # 식약처 impl (M3)
│   │   │   └── ingest.ts                 # 통합 ingest orchestrator (M3)
│   │   ├── mcp/
│   │   │   ├── server.ts                 # MCP server factory (M5)
│   │   │   └── tools/
│   │   │       ├── search-drug.ts        (M5)
│   │   │       ├── get-drug.ts           (M5)
│   │   │       └── chain-drug-dossier.ts (M5)
│   │   └── types.ts                      # Domain types (M1)
│   └── components/
│       ├── ui/                           # shadcn auto-generated (M0)
│       ├── SearchForm.tsx                (M4)
│       ├── DrugCard.tsx                  (M4)
│       └── DrugDetail.tsx                (M4)
├── tests/
│   ├── sources/
│   │   ├── base.test.ts                  (M2)
│   │   └── mfds.test.ts                  (M3)
│   ├── mcp/
│   │   └── tools.test.ts                 (M5)
│   └── fixtures/
│       ├── mfds-search-aspirin.json      (M3)
│       └── mfds-detail-wegovy.json       (M3)
└── e2e/
    └── acceptance-wegovy.spec.ts         # Playwright acceptance (M8)
```

---

## Prerequisites (수동, 사용자 작업)

다음 항목은 사용자가 plan 시작 전에 수동으로 완료해야 함. Bash/Edit으로 처리 불가.

- [ ] **P1: 식약처 OpenAPI 키 발급**

방문: https://www.data.go.kr/data/15095677/openapi.do (의약품안전나라 — 의약품제품허가정보)
- 회원가입 → 로그인 → 활용신청 → 승인 (자동, 즉시) → 키 발급
- 받은 service key 저장: `.env.local`의 `MFDS_SERVICE_KEY`로 박을 예정

- [ ] **P2: Neon Postgres 프로젝트 생성**

방문: https://console.neon.tech → New Project → "koregx" → region = AWS ap-northeast-2 (Seoul) 또는 us-east
- pgvector extension 활성화: Project Settings → Extensions → pgvector enable
- Connection string 저장 (`DATABASE_URL` 형태)

- [ ] **P3: Clerk 프로젝트 생성**

방문: https://dashboard.clerk.com → Create Application → "KoRegX"
- Authentication providers: Email + Google (선택)
- API Keys 저장 (`CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`)

- [ ] **P4: Gemini API 키 (이미 있음)**

기존 `GEMINI_API_KEY` env 재사용.

- [ ] **P5: 도메인 후보 가용성 확인**

다음 3개 중 하나 구매 (M6 deploy 시 사용):
- `koregx.com` (~$10/yr)
- `koregx.io` (~$30/yr)
- `koregx.dev` (~$15/yr, Google Domain)

미리 사지 않아도 Vercel `*.vercel.app` 임시 도메인으로 ship 가능. 도메인 구매는 M6에서 결정.

---

## M0: Project Scaffold

### Task M0-1: Next.js 16 프로젝트 초기화

**Files:**
- Create: `package.json`, `tsconfig.json`, `next.config.ts`, `eslint.config.mjs`, `postcss.config.mjs`, `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/globals.css`

- [ ] **Step 1: Next.js create-next-app 실행 (현재 폴더에)**

```bash
cd "D:/Google Drive/2_user-biz/koregx"
npx create-next-app@latest . --ts --tailwind --eslint --app --src-dir --import-alias "@/*" --no-turbopack --use-npm
```

선택 프롬프트 응답: 모두 default. 기존 README.md / .gitignore overwrite 묻는 경우 **N (no)** 선택.

- [ ] **Step 2: 빌드 확인**

```bash
npm run build
```

Expected: `✓ Compiled successfully`. 에러 없이 통과.

- [ ] **Step 3: dev 서버 시작 확인**

```bash
npm run dev -- --port 3300
```

Expected: `Ready in <Ns>` + `http://localhost:3300` 접근 시 Next.js welcome page.

`Ctrl+C`로 종료.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "M0-1: Next.js 16 scaffold (App Router + TS + Tailwind)"
```

### Task M0-2: shadcn/ui 초기화

**Files:**
- Create: `components.json`, `src/components/ui/*`

- [ ] **Step 1: shadcn init**

```bash
npx shadcn@latest init
```

응답: TypeScript=Y, default style=Default, base color=Slate, css variables=Yes.

- [ ] **Step 2: 기본 컴포넌트 추가 (button, input, card, badge)**

```bash
npx shadcn@latest add button input card badge
```

Expected: `src/components/ui/{button,input,card,badge}.tsx` 생성.

- [ ] **Step 3: 빌드 확인**

```bash
npm run build
```

Expected: 통과.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "M0-2: shadcn/ui init + button/input/card/badge"
```

### Task M0-3: Clerk 설치 + middleware

**Files:**
- Modify: `package.json`, `src/middleware.ts` (create), `src/app/layout.tsx`, `.env.example` (create)

- [ ] **Step 1: Clerk 패키지 설치**

```bash
npm install @clerk/nextjs
```

- [ ] **Step 2: `.env.example` 작성**

```bash
cat > .env.example <<'EOF'
# Clerk
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Postgres (Neon)
DATABASE_URL=

# Gemini
GEMINI_API_KEY=

# 식약처
MFDS_SERVICE_KEY=
EOF
```

`.env.local`에 실제 값 채우기 (P1-P4에서 발급받은 값).

- [ ] **Step 3: middleware.ts 작성**

`src/middleware.ts`:

```typescript
import { clerkMiddleware, createRouteMatcher } from '@clerk/nextjs/server'

const isPublicRoute = createRouteMatcher([
  '/',
  '/sign-in(.*)',
  '/sign-up(.*)',
  '/api/mcp(.*)',  // MCP는 자체 인증
])

export default clerkMiddleware(async (auth, req) => {
  if (!isPublicRoute(req)) {
    await auth.protect()
  }
})

export const config = {
  matcher: [
    '/((?!_next|[^?]*\\.(?:html?|css|js(?!on)|jpe?g|webp|png|gif|svg|ttf|woff2?|ico|csv|docx?|xlsx?|zip|webmanifest)).*)',
    '/(api|trpc)(.*)',
  ],
}
```

- [ ] **Step 4: layout.tsx에 ClerkProvider 추가**

`src/app/layout.tsx`:

```typescript
import { ClerkProvider } from '@clerk/nextjs'
import type { Metadata } from 'next'
import './globals.css'

export const metadata: Metadata = {
  title: 'KoRegX',
  description: 'Korea-anchored cross-region pharma regulatory intelligence',
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <ClerkProvider>
      <html lang="ko" suppressHydrationWarning>
        <body suppressHydrationWarning>{children}</body>
      </html>
    </ClerkProvider>
  )
}
```

- [ ] **Step 5: 빌드 확인**

```bash
npm run build
```

Expected: 통과 (Clerk 키 없어도 빌드는 됨, runtime에서만 필요).

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "M0-3: Clerk 설치 + middleware + ClerkProvider"
```

### Task M0-4: 랜딩 페이지

**Files:**
- Modify: `src/app/page.tsx`

- [ ] **Step 1: page.tsx 작성**

`src/app/page.tsx`:

```typescript
import Link from 'next/link'
import { Button } from '@/components/ui/button'

export default function Home() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center gap-8 p-8">
      <div className="max-w-2xl text-center space-y-4">
        <h1 className="text-5xl font-bold tracking-tight">KoRegX</h1>
        <p className="text-xl text-muted-foreground">
          Korea-anchored cross-region pharma regulatory intelligence
        </p>
        <p className="text-muted-foreground">
          한국·FDA·EMA·PMDA·NMPA의 의약품 허가·급여·규제 정보를 단일 인터페이스에서.
        </p>
      </div>
      <div className="flex gap-4">
        <Button asChild>
          <Link href="/search">의약품 검색</Link>
        </Button>
        <Button asChild variant="outline">
          <Link href="https://github.com/drmoony/koregx" target="_blank">GitHub</Link>
        </Button>
      </div>
    </main>
  )
}
```

- [ ] **Step 2: dev 확인**

```bash
npm run dev -- --port 3300
```

브라우저: `http://localhost:3300` → KoRegX 랜딩 표시 확인. `Ctrl+C` 종료.

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "M0-4: 랜딩 페이지"
```

---

## M1: Database Schema

### Task M1-1: Prisma 7 + driver adapter 설치

**Files:**
- Create: `prisma/schema.prisma`, `src/lib/db.ts`, `prisma.config.ts`

- [ ] **Step 1: 패키지 설치**

```bash
npm install prisma @prisma/client @prisma/adapter-neon @neondatabase/serverless
npm install -D tsx
```

- [ ] **Step 2: prisma.config.ts 작성**

`prisma.config.ts` (root):

```typescript
import path from 'node:path'
import type { PrismaConfig } from 'prisma'
import 'dotenv/config'

export default {
  earlyAccess: true,
  schema: path.join('prisma', 'schema.prisma'),
} satisfies PrismaConfig
```

- [ ] **Step 3: schema.prisma 작성**

`prisma/schema.prisma`:

```prisma
generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["driverAdapters", "postgresqlExtensions"]
}

datasource db {
  provider   = "postgresql"
  url        = env("DATABASE_URL")
  extensions = [pgvector(map: "vector")]
}

model Drug {
  id            String   @id @default(cuid())
  kfdaCode      String?  @unique  // 식약처 표준코드 (의약품 분류번호)
  itemSeq       String?  @unique  // 식약처 품목기준코드 (item sequence)
  productName   String                // 제품명
  ingredient    String?               // 활성성분 (INN 또는 KP 명)
  manufacturer  String?               // 제조/수입사
  atc           String?               // ATC code (있으면)
  category      String?               // 분류 (전문/일반)
  status        String   @default("active")  // active | withdrawn | suspended
  rawData       Json?                 // 식약처 원본 응답 보존
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  approvals     Approval[]
  sourceDocs    SourceDocument[]

  @@index([productName])
  @@index([ingredient])
}

model Approval {
  id              String   @id @default(cuid())
  drugId          String
  drug            Drug     @relation(fields: [drugId], references: [id], onDelete: Cascade)
  region          String                // KR | FDA | EMA | PMDA | NMPA
  authority       String                // MFDS | FDA-CDER | EMA-CHMP | ...
  approvalDate    DateTime?             // 최초 허가일
  approvalType    String?               // NDA | BLA | 신약 | 복제약 | ...
  indication      String?               // 적응증
  status          String   @default("active")
  rawData         Json?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@unique([drugId, region])
  @@index([region, approvalDate])
}

model SourceDocument {
  id          String   @id @default(cuid())
  drugId      String?
  drug        Drug?    @relation(fields: [drugId], references: [id], onDelete: SetNull)
  source      String                // mfds | hira | mohw | kpis | openfda | ...
  url         String                // 출처 URL
  title       String?
  contentHash String                // SHA256 of raw content
  fetchedAt   DateTime @default(now())
  parsedAt    DateTime?
  parser      String?               // parser plugin name (e.g. "mfds.detail.v1")
  metadata    Json?
  createdAt   DateTime @default(now())

  @@unique([source, contentHash])
  @@index([source, fetchedAt])
}
```

- [ ] **Step 4: lib/db.ts 작성 (driver adapter pattern, PharmaNova 검증된 패턴)**

`src/lib/db.ts`:

```typescript
import { PrismaPg } from '@prisma/adapter-neon'
import { Pool, neonConfig } from '@neondatabase/serverless'
import { PrismaClient } from '@prisma/client'
import ws from 'ws'

neonConfig.webSocketConstructor = ws

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined
}

function makeClient() {
  const pool = new Pool({ connectionString: process.env.DATABASE_URL })
  const adapter = new PrismaPg(pool)
  return new PrismaClient({ adapter })
}

export const prisma = globalForPrisma.prisma ?? makeClient()

if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma
```

- [ ] **Step 5: ws 패키지 설치**

```bash
npm install ws
npm install -D @types/ws
```

- [ ] **Step 6: 첫 migration 생성 (DATABASE_URL 필수)**

P2에서 받은 Neon connection string을 `.env.local`의 `DATABASE_URL`에 박아둔 상태에서:

```bash
npx prisma migrate dev --name init
```

Expected: `prisma/migrations/<timestamp>_init/migration.sql` 생성, DB에 테이블 생성. pgvector extension 자동 활성화 시도.

만약 pgvector extension permission 에러 → Neon Console에서 수동 활성화 (P2 단계로 돌아가기).

- [ ] **Step 7: Prisma 클라이언트 generate 확인**

```bash
npx prisma generate
```

Expected: `Generated Prisma Client (...) to ./node_modules/@prisma/client`

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "M1-1: Prisma schema + driver adapter + first migration (Drug/Approval/SourceDocument)"
```

### Task M1-2: 도메인 타입 + 유틸

**Files:**
- Create: `src/lib/types.ts`

- [ ] **Step 1: types.ts 작성**

`src/lib/types.ts`:

```typescript
import type { Drug, Approval, SourceDocument } from '@prisma/client'

export type Region = 'KR' | 'FDA' | 'EMA' | 'PMDA' | 'NMPA'

export type DrugWithApprovals = Drug & {
  approvals: Approval[]
}

export type DrugWithFullDetail = Drug & {
  approvals: Approval[]
  sourceDocs: SourceDocument[]
}

export interface DrugSearchQuery {
  q: string                     // 검색어 (제품명, 성분명, ATC)
  region?: Region
  limit?: number                // default 20
  offset?: number               // default 0
}

export interface DrugSearchResult {
  drugs: DrugWithApprovals[]
  total: number
  limit: number
  offset: number
}

export interface IngestResult {
  source: string
  fetched: number               // API에서 받은 row 수
  inserted: number              // DB에 새로 들어간 수
  updated: number               // 기존 row 업데이트 수
  errors: Array<{ id?: string; error: string }>
  durationMs: number
}
```

- [ ] **Step 2: 빌드 확인**

```bash
npm run build
```

Expected: 통과 (Prisma generate 후라 타입 import 가능).

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "M1-2: 도메인 타입 (Region, DrugSearchQuery, IngestResult)"
```

---

## M2: Source Plugin Abstract Base

### Task M2-1: SourcePlugin 추상 base + 테스트

**Files:**
- Create: `src/lib/sources/base.ts`, `tests/sources/base.test.ts`, `vitest.config.ts`

- [ ] **Step 1: vitest 설치**

```bash
npm install -D vitest @vitest/ui
```

- [ ] **Step 2: vitest.config.ts**

```typescript
import { defineConfig } from 'vitest/config'
import path from 'node:path'

export default defineConfig({
  test: {
    environment: 'node',
    include: ['tests/**/*.test.ts'],
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src'),
    },
  },
})
```

- [ ] **Step 3: package.json에 test script 추가**

`package.json` scripts에:
```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 4: 실패 테스트 작성**

`tests/sources/base.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { SourcePlugin } from '@/lib/sources/base'

describe('SourcePlugin abstract base', () => {
  it('exposes name + region from subclass', () => {
    class TestSource extends SourcePlugin {
      name = 'test'
      region = 'KR' as const
      async fetch() { return [] }
      async parse(raw: unknown) { return [] }
    }
    const s = new TestSource()
    expect(s.name).toBe('test')
    expect(s.region).toBe('KR')
  })

  it('runs fetch -> parse -> persist orchestration', async () => {
    const persisted: unknown[] = []
    class TestSource extends SourcePlugin {
      name = 'test'
      region = 'KR' as const
      async fetch() { return [{ id: 1 }, { id: 2 }] }
      async parse(raw: unknown) { return [{ parsed: (raw as { id: number }).id }] }
      async persist(items: unknown[]) { persisted.push(...items); return { inserted: items.length, updated: 0 } }
    }
    const s = new TestSource()
    const result = await s.run()
    expect(persisted).toEqual([{ parsed: 1 }, { parsed: 2 }])
    expect(result.fetched).toBe(2)
    expect(result.inserted).toBe(2)
    expect(result.errors).toEqual([])
  })

  it('captures errors per item without aborting', async () => {
    class TestSource extends SourcePlugin {
      name = 'test'
      region = 'KR' as const
      async fetch() { return [{ id: 1 }, { id: 2 }, { id: 3 }] }
      async parse(raw: unknown) {
        if ((raw as { id: number }).id === 2) throw new Error('parse fail #2')
        return [{ ok: (raw as { id: number }).id }]
      }
      async persist(items: unknown[]) { return { inserted: items.length, updated: 0 } }
    }
    const s = new TestSource()
    const result = await s.run()
    expect(result.fetched).toBe(3)
    expect(result.inserted).toBe(2)  // 1, 3
    expect(result.errors).toHaveLength(1)
    expect(result.errors[0].error).toContain('parse fail #2')
  })
})
```

- [ ] **Step 5: 테스트 실행 (실패 확인)**

```bash
npm run test
```

Expected: FAIL with `Cannot find module '@/lib/sources/base'`.

- [ ] **Step 6: SourcePlugin base 구현**

`src/lib/sources/base.ts`:

```typescript
import type { Region, IngestResult } from '@/lib/types'

export abstract class SourcePlugin {
  abstract name: string
  abstract region: Region

  abstract fetch(...args: unknown[]): Promise<unknown[]>
  abstract parse(raw: unknown): Promise<unknown[]>

  async persist(items: unknown[]): Promise<{ inserted: number; updated: number }> {
    return { inserted: 0, updated: 0 }
  }

  async run(...args: unknown[]): Promise<IngestResult> {
    const start = Date.now()
    const errors: Array<{ id?: string; error: string }> = []
    let fetched = 0
    let allParsed: unknown[] = []

    try {
      const rawItems = await this.fetch(...args)
      fetched = rawItems.length
      for (const raw of rawItems) {
        try {
          const parsed = await this.parse(raw)
          allParsed.push(...parsed)
        } catch (e) {
          errors.push({ error: e instanceof Error ? e.message : String(e) })
        }
      }
    } catch (e) {
      errors.push({ error: `fetch failed: ${e instanceof Error ? e.message : String(e)}` })
    }

    let inserted = 0
    let updated = 0
    if (allParsed.length > 0) {
      const persistResult = await this.persist(allParsed)
      inserted = persistResult.inserted
      updated = persistResult.updated
    }

    return {
      source: this.name,
      fetched,
      inserted,
      updated,
      errors,
      durationMs: Date.now() - start,
    }
  }
}
```

- [ ] **Step 7: 테스트 실행 (통과 확인)**

```bash
npm run test
```

Expected: 3 passed.

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "M2-1: SourcePlugin abstract base + run() orchestration tests"
```

---

## M3: 식약처 OpenAPI 통합

### Task M3-1: 식약처 fixtures 캡처

**Files:**
- Create: `tests/fixtures/mfds-search-aspirin.json`, `tests/fixtures/mfds-detail-wegovy.json`

- [ ] **Step 1: 식약처 검색 응답 fixture (수동 fetch 후 저장)**

P1에서 받은 service key를 사용해 한 번 호출:

```bash
SERVICE_KEY="<your-mfds-service-key>"
curl -s "https://apis.data.go.kr/1471000/MdcinGrnIdntfcInfoService02/getMdcinGrnIdntfcInfoList02?serviceKey=${SERVICE_KEY}&item_name=아스피린&type=json&numOfRows=5" \
  > tests/fixtures/mfds-search-aspirin.json
```

응답이 `{ "body": { "items": [...] } }` 형태인지 확인. 빈 응답 또는 에러면 service key 또는 endpoint 재확인.

- [ ] **Step 2: Wegovy 상세 fixture**

```bash
curl -s "https://apis.data.go.kr/1471000/MdcinGrnIdntfcInfoService02/getMdcinGrnIdntfcInfoList02?serviceKey=${SERVICE_KEY}&item_name=위고비&type=json&numOfRows=5" \
  > tests/fixtures/mfds-detail-wegovy.json
```

- [ ] **Step 3: fixture 내용 점검**

```bash
node -e "console.log(JSON.stringify(require('./tests/fixtures/mfds-search-aspirin.json').body.items[0], null, 2))"
```

Expected: 첫 item이 `{ ITEM_NAME, ITEM_SEQ, ENTP_NAME, MAIN_INGR, ... }` 같은 필드 포함. 이 필드명을 다음 step parser 매핑 기준으로 사용.

⚠️ **만약 응답 형태가 다르면** (예: XML, 또는 필드명 다름): Step 4의 mapping 함수를 실제 응답에 맞춰 조정 필요. 식약처 OpenAPI는 시점에 따라 형식이 변경될 수 있으므로 fixture가 source of truth.

- [ ] **Step 4: Commit**

```bash
git add tests/fixtures/
git commit -m "M3-1: 식약처 API fixtures (아스피린 검색, 위고비 검색)"
```

### Task M3-2: 식약처 source plugin + 테스트

**Files:**
- Create: `src/lib/sources/mfds.ts`, `tests/sources/mfds.test.ts`

- [ ] **Step 1: 실패 테스트 작성**

`tests/sources/mfds.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { MfdsSource, parseMfdsItem } from '@/lib/sources/mfds'
import searchFixture from '../fixtures/mfds-search-aspirin.json'

describe('parseMfdsItem', () => {
  it('maps 식약처 item to Drug shape', () => {
    const item = (searchFixture as { body: { items: unknown[] } }).body.items[0] as Record<string, string>
    const drug = parseMfdsItem(item)
    expect(drug.productName).toBe(item.ITEM_NAME)
    expect(drug.itemSeq).toBe(item.ITEM_SEQ)
    expect(drug.manufacturer).toBe(item.ENTP_NAME)
    expect(drug.region).toBe('KR')
    expect(drug.rawData).toEqual(item)
  })

  it('handles missing optional fields gracefully', () => {
    const minimal = { ITEM_NAME: '테스트정', ITEM_SEQ: '999999' }
    const drug = parseMfdsItem(minimal)
    expect(drug.productName).toBe('테스트정')
    expect(drug.ingredient).toBeUndefined()
    expect(drug.manufacturer).toBeUndefined()
  })
})

describe('MfdsSource', () => {
  beforeEach(() => {
    vi.restoreAllMocks()
  })

  it('fetches with service key and parses items', async () => {
    const fetchSpy = vi.spyOn(globalThis, 'fetch').mockResolvedValue(
      new Response(JSON.stringify(searchFixture), { status: 200 })
    )
    const source = new MfdsSource({ serviceKey: 'test-key' })
    const items = await source.fetch({ query: '아스피린', limit: 5 })
    expect(fetchSpy).toHaveBeenCalled()
    const url = fetchSpy.mock.calls[0][0] as string
    expect(url).toContain('serviceKey=test-key')
    expect(url).toContain('item_name=')
    expect(items.length).toBeGreaterThan(0)
  })

  it('throws on HTTP error', async () => {
    vi.spyOn(globalThis, 'fetch').mockResolvedValue(new Response('upstream error', { status: 500 }))
    const source = new MfdsSource({ serviceKey: 'test-key' })
    await expect(source.fetch({ query: 'X', limit: 1 })).rejects.toThrow(/MFDS fetch failed/)
  })
})
```

- [ ] **Step 2: 실행 (실패 확인)**

```bash
npm run test
```

Expected: FAIL with `Cannot find module '@/lib/sources/mfds'`.

- [ ] **Step 3: MfdsSource 구현**

`src/lib/sources/mfds.ts`:

```typescript
import { SourcePlugin } from './base'
import type { Region } from '@/lib/types'

const MFDS_BASE_URL = 'https://apis.data.go.kr/1471000/MdcinGrnIdntfcInfoService02/getMdcinGrnIdntfcInfoList02'

export interface ParsedDrug {
  region: Region
  itemSeq?: string
  productName: string
  ingredient?: string
  manufacturer?: string
  category?: string
  rawData: Record<string, unknown>
}

export function parseMfdsItem(item: Record<string, unknown>): ParsedDrug {
  const get = (k: string) => {
    const v = item[k]
    return typeof v === 'string' && v.trim() !== '' ? v.trim() : undefined
  }
  return {
    region: 'KR',
    itemSeq: get('ITEM_SEQ'),
    productName: get('ITEM_NAME') ?? '(unknown)',
    ingredient: get('MAIN_INGR') ?? get('GNL_NM'),
    manufacturer: get('ENTP_NAME'),
    category: get('CLASS_NAME') ?? get('ETC_OTC_CODE'),
    rawData: item,
  }
}

export interface MfdsFetchArgs {
  query: string
  limit?: number
}

export class MfdsSource extends SourcePlugin {
  name = 'mfds'
  region = 'KR' as const
  serviceKey: string

  constructor(opts: { serviceKey: string }) {
    super()
    this.serviceKey = opts.serviceKey
  }

  async fetch(args: MfdsFetchArgs): Promise<unknown[]> {
    const params = new URLSearchParams({
      serviceKey: this.serviceKey,
      item_name: args.query,
      type: 'json',
      numOfRows: String(args.limit ?? 20),
    })
    const url = `${MFDS_BASE_URL}?${params.toString()}`
    const res = await fetch(url)
    if (!res.ok) throw new Error(`MFDS fetch failed: ${res.status}`)
    const json = (await res.json()) as { body?: { items?: unknown[] } }
    return json.body?.items ?? []
  }

  async parse(raw: unknown): Promise<ParsedDrug[]> {
    return [parseMfdsItem(raw as Record<string, unknown>)]
  }
}
```

- [ ] **Step 4: 실행 (통과 확인)**

```bash
npm run test
```

Expected: 5 passed (M2 3건 + M3 2건 parser + 2건 source).

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "M3-2: 식약처 source plugin + parseMfdsItem + tests"
```

### Task M3-3: 식약처 ingest service (DB persist)

**Files:**
- Create: `src/lib/sources/ingest.ts`
- Modify: `src/lib/sources/mfds.ts` (persist 메서드 추가)

- [ ] **Step 1: 실패 테스트 작성 (mfds.test.ts에 추가)**

`tests/sources/mfds.test.ts` 파일 **하단에** 추가:

```typescript
import { prisma } from '@/lib/db'

describe('MfdsSource.persist', () => {
  beforeEach(async () => {
    // 테스트 환경에서만 실행. 실제 prod DB 보호 위해 NODE_ENV 체크
    if (process.env.NODE_ENV === 'production') throw new Error('persist test cannot run in production')
    await prisma.approval.deleteMany({})
    await prisma.drug.deleteMany({ where: { itemSeq: { startsWith: 'TEST_' } } })
  })

  it('upserts Drug + Approval and creates SourceDocument', async () => {
    const parsed = [{
      region: 'KR' as const,
      itemSeq: 'TEST_001',
      productName: '테스트정',
      ingredient: '테스트성분',
      manufacturer: '테스트사',
      rawData: { ITEM_SEQ: 'TEST_001', ITEM_NAME: '테스트정' },
    }]
    const source = new MfdsSource({ serviceKey: 'test' })
    const result = await source.persist(parsed)
    expect(result.inserted).toBe(1)
    expect(result.updated).toBe(0)

    const drug = await prisma.drug.findUnique({ where: { itemSeq: 'TEST_001' }, include: { approvals: true } })
    expect(drug).toBeTruthy()
    expect(drug?.productName).toBe('테스트정')
    expect(drug?.approvals).toHaveLength(1)
    expect(drug?.approvals[0].region).toBe('KR')
  })

  it('updates existing drug instead of duplicating', async () => {
    const parsed1 = [{ region: 'KR' as const, itemSeq: 'TEST_002', productName: '오리지날', rawData: {} }]
    const parsed2 = [{ region: 'KR' as const, itemSeq: 'TEST_002', productName: '갱신된이름', rawData: {} }]
    const source = new MfdsSource({ serviceKey: 'test' })
    await source.persist(parsed1)
    const r2 = await source.persist(parsed2)
    expect(r2.updated).toBe(1)
    expect(r2.inserted).toBe(0)

    const drug = await prisma.drug.findUnique({ where: { itemSeq: 'TEST_002' } })
    expect(drug?.productName).toBe('갱신된이름')
  })
})
```

- [ ] **Step 2: 실행 (실패 확인)**

```bash
npm run test
```

Expected: FAIL — `persist` 메서드가 base의 noop을 호출해서 inserted=0.

- [ ] **Step 3: MfdsSource.persist 구현**

`src/lib/sources/mfds.ts` 파일 **하단의 class 안**에 추가:

```typescript
import { prisma } from '@/lib/db'

// ... 기존 class MfdsSource extends SourcePlugin { ... } 안에서:

  async persist(items: ParsedDrug[]): Promise<{ inserted: number; updated: number }> {
    let inserted = 0
    let updated = 0
    for (const item of items) {
      if (!item.itemSeq) continue  // skip without natural key
      const existing = await prisma.drug.findUnique({ where: { itemSeq: item.itemSeq } })
      const drug = await prisma.drug.upsert({
        where: { itemSeq: item.itemSeq },
        create: {
          itemSeq: item.itemSeq,
          productName: item.productName,
          ingredient: item.ingredient,
          manufacturer: item.manufacturer,
          category: item.category,
          rawData: item.rawData,
        },
        update: {
          productName: item.productName,
          ingredient: item.ingredient,
          manufacturer: item.manufacturer,
          category: item.category,
          rawData: item.rawData,
          updatedAt: new Date(),
        },
      })
      if (existing) updated++
      else inserted++

      // Approval upsert (KR axis)
      await prisma.approval.upsert({
        where: { drugId_region: { drugId: drug.id, region: 'KR' } },
        create: {
          drugId: drug.id,
          region: 'KR',
          authority: 'MFDS',
          rawData: item.rawData,
        },
        update: {
          rawData: item.rawData,
          updatedAt: new Date(),
        },
      })
    }
    return { inserted, updated }
  }
```

⚠️ Import는 파일 상단에 추가: `import { prisma } from '@/lib/db'`. 클래스 안 메서드는 prisma를 closure로 access.

- [ ] **Step 4: 실행 (통과 확인)**

```bash
npm run test
```

Expected: 7 passed (M2 3 + M3 parser 2 + source 2 + persist 2).

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "M3-3: MFDS persist (Drug+Approval upsert) + tests"
```

### Task M3-4: 통합 ingest orchestrator + npm script

**Files:**
- Create: `src/lib/sources/ingest.ts`, `scripts/ingest-mfds.ts`
- Modify: `package.json`

- [ ] **Step 1: ingest.ts 작성**

`src/lib/sources/ingest.ts`:

```typescript
import { MfdsSource } from './mfds'
import type { IngestResult } from '@/lib/types'

export async function ingestMfds(query: string, limit = 50): Promise<IngestResult> {
  const serviceKey = process.env.MFDS_SERVICE_KEY
  if (!serviceKey) throw new Error('MFDS_SERVICE_KEY env not set')
  const source = new MfdsSource({ serviceKey })
  return source.run({ query, limit })
}
```

- [ ] **Step 2: 수동 ingest 스크립트**

`scripts/ingest-mfds.ts`:

```typescript
import { ingestMfds } from '@/lib/sources/ingest'

async function main() {
  const query = process.argv[2] ?? '위고비'
  const limit = Number(process.argv[3] ?? 20)
  console.log(`MFDS ingest: query="${query}" limit=${limit}`)
  const result = await ingestMfds(query, limit)
  console.log(JSON.stringify(result, null, 2))
}

main().catch((e) => {
  console.error(e)
  process.exit(1)
})
```

- [ ] **Step 3: package.json에 script 추가**

`package.json` scripts:

```json
"ingest:mfds": "tsx scripts/ingest-mfds.ts"
```

- [ ] **Step 4: 실제 ingest 실행 (Wegovy)**

```bash
npm run ingest:mfds 위고비 10
```

Expected: 콘솔에 `{ source: 'mfds', fetched: N, inserted: M, updated: 0, errors: [], durationMs: ... }` 출력.

- [ ] **Step 5: DB 확인**

```bash
npx prisma studio --port 5555
```

브라우저에서 Drug 테이블 → 위고비 row 확인. 종료.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "M3-4: ingest orchestrator + 수동 스크립트"
```

---

## M4: Web UI — Search + Drug Detail

### Task M4-1: 검색 server action + 페이지

**Files:**
- Create: `src/app/search/page.tsx`, `src/components/SearchForm.tsx`, `src/components/DrugCard.tsx`
- Create: `src/app/search/actions.ts`

- [ ] **Step 1: server action**

`src/app/search/actions.ts`:

```typescript
'use server'

import { prisma } from '@/lib/db'
import type { DrugSearchQuery, DrugSearchResult, DrugWithApprovals } from '@/lib/types'

export async function searchDrugs(q: DrugSearchQuery): Promise<DrugSearchResult> {
  const { q: query, limit = 20, offset = 0 } = q
  if (!query || query.trim().length === 0) {
    return { drugs: [], total: 0, limit, offset }
  }
  const where = {
    OR: [
      { productName: { contains: query, mode: 'insensitive' as const } },
      { ingredient: { contains: query, mode: 'insensitive' as const } },
      { manufacturer: { contains: query, mode: 'insensitive' as const } },
    ],
  }
  const [drugs, total] = await Promise.all([
    prisma.drug.findMany({
      where,
      include: { approvals: true },
      take: limit,
      skip: offset,
      orderBy: { productName: 'asc' },
    }) as Promise<DrugWithApprovals[]>,
    prisma.drug.count({ where }),
  ])
  return { drugs, total, limit, offset }
}
```

- [ ] **Step 2: SearchForm 컴포넌트**

`src/components/SearchForm.tsx`:

```typescript
'use client'

import { useState } from 'react'
import { useRouter, useSearchParams } from 'next/navigation'
import { Input } from '@/components/ui/input'
import { Button } from '@/components/ui/button'

export function SearchForm() {
  const router = useRouter()
  const searchParams = useSearchParams()
  const [q, setQ] = useState(searchParams.get('q') ?? '')

  function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    if (!q.trim()) return
    router.push(`/search?q=${encodeURIComponent(q.trim())}`)
  }

  return (
    <form onSubmit={handleSubmit} className="flex gap-2 max-w-xl">
      <Input
        type="search"
        placeholder="제품명·성분·제조사 검색 (예: 위고비)"
        value={q}
        onChange={(e) => setQ(e.target.value)}
        className="flex-1"
      />
      <Button type="submit">검색</Button>
    </form>
  )
}
```

- [ ] **Step 3: DrugCard 컴포넌트**

`src/components/DrugCard.tsx`:

```typescript
import Link from 'next/link'
import type { DrugWithApprovals } from '@/lib/types'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'

export function DrugCard({ drug }: { drug: DrugWithApprovals }) {
  return (
    <Link href={`/drug/${drug.id}`}>
      <Card className="hover:bg-accent/50 transition-colors">
        <CardHeader>
          <CardTitle className="text-lg">{drug.productName}</CardTitle>
        </CardHeader>
        <CardContent className="space-y-2">
          {drug.ingredient && (
            <p className="text-sm text-muted-foreground">성분: {drug.ingredient}</p>
          )}
          {drug.manufacturer && (
            <p className="text-sm text-muted-foreground">제조: {drug.manufacturer}</p>
          )}
          <div className="flex gap-1 flex-wrap">
            {drug.approvals.map((a) => (
              <Badge key={a.id} variant="outline">{a.region}</Badge>
            ))}
          </div>
        </CardContent>
      </Card>
    </Link>
  )
}
```

- [ ] **Step 4: 검색 페이지**

`src/app/search/page.tsx`:

```typescript
import { SearchForm } from '@/components/SearchForm'
import { DrugCard } from '@/components/DrugCard'
import { searchDrugs } from './actions'

export default async function SearchPage({ searchParams }: { searchParams: Promise<{ q?: string }> }) {
  const params = await searchParams
  const q = params.q ?? ''
  const result = q ? await searchDrugs({ q }) : null

  return (
    <main className="container mx-auto py-8 space-y-8 max-w-4xl">
      <div>
        <h1 className="text-3xl font-bold mb-4">의약품 검색</h1>
        <SearchForm />
      </div>
      {result && (
        <div className="space-y-2">
          <p className="text-sm text-muted-foreground">
            {result.total === 0 ? '검색 결과 없음' : `${result.total}건`}
          </p>
          <div className="grid gap-3">
            {result.drugs.map((drug) => (
              <DrugCard key={drug.id} drug={drug} />
            ))}
          </div>
        </div>
      )}
    </main>
  )
}
```

- [ ] **Step 5: dev 확인**

```bash
npm run dev -- --port 3300
```

브라우저: `http://localhost:3300/search?q=위고비` → DrugCard 표시. `Ctrl+C`.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "M4-1: 검색 페이지 + SearchForm + DrugCard"
```

### Task M4-2: 약물 상세 페이지

**Files:**
- Create: `src/app/drug/[id]/page.tsx`, `src/components/DrugDetail.tsx`

- [ ] **Step 1: DrugDetail 컴포넌트**

`src/components/DrugDetail.tsx`:

```typescript
import type { DrugWithFullDetail } from '@/lib/types'
import { Badge } from '@/components/ui/badge'

export function DrugDetail({ drug }: { drug: DrugWithFullDetail }) {
  return (
    <div className="space-y-6">
      <div>
        <h1 className="text-3xl font-bold">{drug.productName}</h1>
        {drug.ingredient && (
          <p className="text-lg text-muted-foreground mt-2">성분: {drug.ingredient}</p>
        )}
      </div>

      <section className="space-y-2">
        <h2 className="text-xl font-semibold">기본 정보</h2>
        <dl className="grid grid-cols-2 gap-2 text-sm">
          {drug.manufacturer && (
            <>
              <dt className="text-muted-foreground">제조/수입사</dt>
              <dd>{drug.manufacturer}</dd>
            </>
          )}
          {drug.category && (
            <>
              <dt className="text-muted-foreground">분류</dt>
              <dd>{drug.category}</dd>
            </>
          )}
          {drug.itemSeq && (
            <>
              <dt className="text-muted-foreground">식약처 품목코드</dt>
              <dd className="font-mono">{drug.itemSeq}</dd>
            </>
          )}
          <dt className="text-muted-foreground">상태</dt>
          <dd>{drug.status}</dd>
        </dl>
      </section>

      <section className="space-y-2">
        <h2 className="text-xl font-semibold">허가 (Approvals)</h2>
        <div className="space-y-2">
          {drug.approvals.map((a) => (
            <div key={a.id} className="border rounded p-3 space-y-1">
              <div className="flex items-center gap-2">
                <Badge>{a.region}</Badge>
                <span className="text-sm text-muted-foreground">{a.authority}</span>
              </div>
              {a.indication && <p className="text-sm">{a.indication}</p>}
              {a.approvalDate && (
                <p className="text-xs text-muted-foreground">
                  허가일: {new Date(a.approvalDate).toLocaleDateString('ko-KR')}
                </p>
              )}
            </div>
          ))}
          {drug.approvals.length === 0 && (
            <p className="text-sm text-muted-foreground">허가 데이터 없음</p>
          )}
        </div>
      </section>

      <section className="space-y-2">
        <h2 className="text-xl font-semibold">출처 문서</h2>
        <p className="text-sm text-muted-foreground">
          {drug.sourceDocs.length}건의 출처 문서
        </p>
      </section>
    </div>
  )
}
```

- [ ] **Step 2: 상세 페이지**

`src/app/drug/[id]/page.tsx`:

```typescript
import { notFound } from 'next/navigation'
import { prisma } from '@/lib/db'
import { DrugDetail } from '@/components/DrugDetail'

export default async function DrugPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params
  const drug = await prisma.drug.findUnique({
    where: { id },
    include: { approvals: true, sourceDocs: true },
  })
  if (!drug) notFound()

  return (
    <main className="container mx-auto py-8 max-w-4xl">
      <DrugDetail drug={drug} />
    </main>
  )
}
```

- [ ] **Step 3: dev 확인 — 검색 → 클릭 → 상세 → 허가 정보 표시**

```bash
npm run dev -- --port 3300
```

`http://localhost:3300/search?q=위고비` → 카드 클릭 → `/drug/<id>` 표시 확인.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "M4-2: 약물 상세 페이지"
```

---

## M5: MCP Server — 3 Tools

### Task M5-1: MCP SDK 설치 + 기본 구조 + 테스트

**Files:**
- Create: `src/lib/mcp/server.ts`, `src/lib/mcp/tools/search-drug.ts`, `src/lib/mcp/tools/get-drug.ts`, `src/lib/mcp/tools/chain-drug-dossier.ts`, `tests/mcp/tools.test.ts`

- [ ] **Step 1: MCP SDK 설치**

```bash
npm install @modelcontextprotocol/sdk zod
```

- [ ] **Step 2: search-drug tool 실패 테스트**

`tests/mcp/tools.test.ts`:

```typescript
import { describe, it, expect, beforeAll, beforeEach } from 'vitest'
import { searchDrugTool } from '@/lib/mcp/tools/search-drug'
import { getDrugTool } from '@/lib/mcp/tools/get-drug'
import { chainDrugDossierTool } from '@/lib/mcp/tools/chain-drug-dossier'
import { prisma } from '@/lib/db'

beforeAll(async () => {
  // 테스트용 fixture drug 생성
  await prisma.drug.upsert({
    where: { itemSeq: 'TEST_MCP_001' },
    create: {
      itemSeq: 'TEST_MCP_001',
      productName: '위고비테스트',
      ingredient: 'semaglutide',
      manufacturer: 'NovoNordisk',
      approvals: { create: { region: 'KR', authority: 'MFDS' } },
    },
    update: {},
  })
})

describe('search_drug tool', () => {
  it('returns matching drugs by product name', async () => {
    const result = await searchDrugTool.execute({ query: '위고비테스트' })
    expect(result.drugs.length).toBeGreaterThan(0)
    expect(result.drugs[0].productName).toContain('위고비테스트')
  })

  it('returns empty for no match', async () => {
    const result = await searchDrugTool.execute({ query: '존재하지않는약물' })
    expect(result.drugs).toEqual([])
  })

  it('respects limit', async () => {
    const result = await searchDrugTool.execute({ query: '위고비', limit: 1 })
    expect(result.drugs.length).toBeLessThanOrEqual(1)
  })
})

describe('get_drug tool', () => {
  it('returns drug with approvals by id', async () => {
    const drug = await prisma.drug.findUnique({ where: { itemSeq: 'TEST_MCP_001' } })
    const result = await getDrugTool.execute({ id: drug!.id })
    expect(result.drug?.productName).toBe('위고비테스트')
    expect(result.drug?.approvals).toHaveLength(1)
  })

  it('returns null for non-existent id', async () => {
    const result = await getDrugTool.execute({ id: 'nonexistent_id' })
    expect(result.drug).toBeNull()
  })
})

describe('chain_drug_dossier tool', () => {
  it('combines search + detail in one call', async () => {
    const result = await chainDrugDossierTool.execute({ query: '위고비테스트' })
    expect(result.summary).toContain('위고비테스트')
    expect(result.matched).toBe(true)
    expect(result.drug?.approvals).toHaveLength(1)
  })

  it('returns matched=false when no result', async () => {
    const result = await chainDrugDossierTool.execute({ query: '존재하지않음' })
    expect(result.matched).toBe(false)
  })
})
```

- [ ] **Step 3: 실행 (실패 확인)**

```bash
npm run test
```

Expected: FAIL with `Cannot find module '@/lib/mcp/tools/search-drug'`.

- [ ] **Step 4: search-drug tool 구현**

`src/lib/mcp/tools/search-drug.ts`:

```typescript
import { z } from 'zod'
import { prisma } from '@/lib/db'

export const searchDrugSchema = z.object({
  query: z.string().min(1).describe('제품명, 성분명, 제조사 키워드'),
  limit: z.number().int().min(1).max(100).default(20).optional(),
})

export const searchDrugTool = {
  name: 'search_drug',
  description: '한국 식약처 의약품 검색 (제품명·성분·제조사). 다지역 검색은 v0.5+',
  inputSchema: searchDrugSchema,
  async execute(args: z.infer<typeof searchDrugSchema>) {
    const { query, limit = 20 } = args
    const drugs = await prisma.drug.findMany({
      where: {
        OR: [
          { productName: { contains: query, mode: 'insensitive' } },
          { ingredient: { contains: query, mode: 'insensitive' } },
          { manufacturer: { contains: query, mode: 'insensitive' } },
        ],
      },
      include: { approvals: true },
      take: limit,
      orderBy: { productName: 'asc' },
    })
    return { drugs, total: drugs.length }
  },
}
```

- [ ] **Step 5: get-drug tool 구현**

`src/lib/mcp/tools/get-drug.ts`:

```typescript
import { z } from 'zod'
import { prisma } from '@/lib/db'

export const getDrugSchema = z.object({
  id: z.string().describe('Drug.id (cuid) 또는 itemSeq (식약처 품목코드)'),
})

export const getDrugTool = {
  name: 'get_drug',
  description: '특정 약물 상세 (허가 + 출처 문서 포함)',
  inputSchema: getDrugSchema,
  async execute(args: z.infer<typeof getDrugSchema>) {
    const { id } = args
    const drug = await prisma.drug.findFirst({
      where: { OR: [{ id }, { itemSeq: id }] },
      include: { approvals: true, sourceDocs: true },
    })
    return { drug }
  },
}
```

- [ ] **Step 6: chain-drug-dossier tool 구현**

`src/lib/mcp/tools/chain-drug-dossier.ts`:

```typescript
import { z } from 'zod'
import { prisma } from '@/lib/db'

export const chainDrugDossierSchema = z.object({
  query: z.string().min(1).describe('약물 검색어'),
})

export const chainDrugDossierTool = {
  name: 'chain_drug_dossier',
  description: '약물 검색 → 첫 매칭 약물의 허가·출처 통합 dossier 반환',
  inputSchema: chainDrugDossierSchema,
  async execute(args: z.infer<typeof chainDrugDossierSchema>) {
    const { query } = args
    const drug = await prisma.drug.findFirst({
      where: {
        OR: [
          { productName: { contains: query, mode: 'insensitive' } },
          { ingredient: { contains: query, mode: 'insensitive' } },
        ],
      },
      include: { approvals: true, sourceDocs: true },
      orderBy: { productName: 'asc' },
    })
    if (!drug) {
      return { matched: false, summary: `"${query}"에 매칭되는 약물 없음`, drug: null }
    }
    const approvalRegions = drug.approvals.map((a) => a.region).join(', ') || '(없음)'
    const summary = `${drug.productName} — ${drug.ingredient ?? '성분 미상'}. 제조: ${drug.manufacturer ?? '미상'}. 허가 region: ${approvalRegions}.`
    return { matched: true, summary, drug }
  },
}
```

- [ ] **Step 7: 실행 (통과 확인)**

```bash
npm run test
```

Expected: 전체 통과 (M2 3 + M3 7 + M5 6 = 16).

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "M5-1: MCP tools (search_drug, get_drug, chain_drug_dossier) + tests"
```

### Task M5-2: MCP server endpoint (HTTP transport)

**Files:**
- Create: `src/lib/mcp/server.ts`, `src/app/api/mcp/route.ts`

- [ ] **Step 1: MCP server factory**

`src/lib/mcp/server.ts`:

```typescript
import { Server } from '@modelcontextprotocol/sdk/server/index.js'
import { CallToolRequestSchema, ListToolsRequestSchema } from '@modelcontextprotocol/sdk/types.js'
import { searchDrugTool } from './tools/search-drug'
import { getDrugTool } from './tools/get-drug'
import { chainDrugDossierTool } from './tools/chain-drug-dossier'
import { z } from 'zod'

const tools = [searchDrugTool, getDrugTool, chainDrugDossierTool]
const toolByName = new Map(tools.map((t) => [t.name, t]))

export function createKoRegXServer() {
  const server = new Server(
    { name: 'koregx', version: '0.1.0' },
    { capabilities: { tools: {} } }
  )

  server.setRequestHandler(ListToolsRequestSchema, async () => ({
    tools: tools.map((t) => ({
      name: t.name,
      description: t.description,
      inputSchema: zodToJsonSchema(t.inputSchema),
    })),
  }))

  server.setRequestHandler(CallToolRequestSchema, async (request) => {
    const { name, arguments: args } = request.params
    const tool = toolByName.get(name)
    if (!tool) throw new Error(`unknown tool: ${name}`)
    const parsed = tool.inputSchema.parse(args)
    const result = await tool.execute(parsed as never)
    return {
      content: [{ type: 'text', text: JSON.stringify(result, null, 2) }],
    }
  })

  return server
}

// Minimal Zod → JSON Schema converter for tool input descriptions
function zodToJsonSchema(schema: z.ZodTypeAny): Record<string, unknown> {
  if (schema instanceof z.ZodObject) {
    const properties: Record<string, unknown> = {}
    const required: string[] = []
    for (const [key, value] of Object.entries(schema.shape)) {
      properties[key] = zodToJsonSchema(value as z.ZodTypeAny)
      if (!(value instanceof z.ZodOptional) && !(value instanceof z.ZodDefault)) {
        required.push(key)
      }
    }
    return { type: 'object', properties, required }
  }
  if (schema instanceof z.ZodString) return { type: 'string', description: schema.description }
  if (schema instanceof z.ZodNumber) return { type: 'number', description: schema.description }
  if (schema instanceof z.ZodBoolean) return { type: 'boolean', description: schema.description }
  if (schema instanceof z.ZodOptional) return zodToJsonSchema(schema._def.innerType)
  if (schema instanceof z.ZodDefault) return zodToJsonSchema(schema._def.innerType)
  return { type: 'string' }
}
```

- [ ] **Step 2: HTTP route — Streamable HTTP transport**

`src/app/api/mcp/route.ts`:

```typescript
import { StreamableHTTPServerTransport } from '@modelcontextprotocol/sdk/server/streamableHttp.js'
import { createKoRegXServer } from '@/lib/mcp/server'

export const dynamic = 'force-dynamic'
export const runtime = 'nodejs'

async function handle(req: Request) {
  const server = createKoRegXServer()
  const transport = new StreamableHTTPServerTransport({
    sessionIdGenerator: undefined,  // stateless mode
  })
  await server.connect(transport)
  const reqInit: RequestInit & { duplex: 'half' } = {
    method: req.method,
    headers: req.headers,
    body: req.body,
    duplex: 'half',
  }
  // 간단한 stateless 응답 처리: SDK는 Request/Response 직접 처리
  // (TypeScript Streamable HTTP SDK >= 0.5.x 기준)
  return await transport.handleHttpRequest(req)
}

export { handle as GET, handle as POST }
```

⚠️ MCP SDK의 Streamable HTTP transport API 시그니처가 버전에 따라 다를 수 있음. 위 코드 작동 안 하면 `npm ls @modelcontextprotocol/sdk`로 버전 확인 후 SDK README 참조해 reroute.

- [ ] **Step 3: dev 확인 — MCP endpoint 응답**

```bash
npm run dev -- --port 3300
```

다른 터미널에서:

```bash
curl -X POST http://localhost:3300/api/mcp \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/list"}'
```

Expected: 3개 tool (search_drug, get_drug, chain_drug_dossier) JSON 응답.

`Ctrl+C`.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "M5-2: MCP server endpoint /api/mcp (Streamable HTTP, 3 tools)"
```

---

## M6: Deploy

### Task M6-1: Vercel 연결 + 환경변수 + 첫 deploy

**Files:**
- Modify: `.gitignore`, `vercel.json` (옵션)

- [ ] **Step 1: Vercel CLI 설치 + 로그인**

```bash
npm install -g vercel
vercel login
```

브라우저 OAuth flow 따라 로그인.

- [ ] **Step 2: 프로젝트 link**

```bash
cd "D:/Google Drive/2_user-biz/koregx"
vercel link
```

응답: scope 선택, project name = `koregx`, overwrite settings = N.

- [ ] **Step 3: 환경변수 등록**

```bash
vercel env add DATABASE_URL production
vercel env add NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY production
vercel env add CLERK_SECRET_KEY production
vercel env add GEMINI_API_KEY production
vercel env add MFDS_SERVICE_KEY production
```

각 prompt에 `.env.local` 값 붙여넣기.

Preview/development 환경에도 동일 등록 (또는 `--scope=development`):

```bash
vercel env add DATABASE_URL preview development
# (반복)
```

- [ ] **Step 4: Production deploy**

```bash
vercel --prod
```

Expected: `✓ Production: https://koregx-xxxx.vercel.app` URL 출력.

- [ ] **Step 5: deploy URL 접근 확인**

브라우저에서 출력된 URL → 랜딩 페이지 확인. `/search?q=위고비` → 빈 결과 (production DB는 비어있음).

- [ ] **Step 6: production DB seed (위고비 ingest)**

production env로 ingest 1회 실행 — 로컬에서 DATABASE_URL을 production 값으로 바꿔 1회만:

```bash
DATABASE_URL="<prod-neon-url>" MFDS_SERVICE_KEY="<key>" npm run ingest:mfds 위고비 20
```

또는 Vercel Functions로 ingest endpoint 만들어 실행 (다음 task).

브라우저 다시 확인 → 검색 결과 보임.

- [ ] **Step 7: Commit (vercel.json 있으면)**

vercel.json은 자동 생성되지 않으므로 commit 불필요. 단 `.gitignore`에 `.vercel/` 있는지 확인:

```bash
echo ".vercel/" >> .gitignore
git add -A
git commit -m "M6-1: Vercel deploy + env vars"
```

### Task M6-2: README 업데이트

**Files:**
- Modify: `README.md`

- [ ] **Step 1: README 추가**

`README.md` 에 다음 섹션 추가 (기존 README 하단):

```markdown
## Live Demo

- Web: https://koregx-xxxx.vercel.app  *(실제 URL로 교체)*
- Search: https://koregx-xxxx.vercel.app/search?q=위고비

## MCP Connector

### Claude Desktop

`%APPDATA%\Claude\claude_desktop_config.json` (Windows) 또는 `~/Library/Application Support/Claude/claude_desktop_config.json` (macOS)에 추가:

```json
{
  "mcpServers": {
    "koregx": {
      "url": "https://koregx-xxxx.vercel.app/api/mcp"
    }
  }
}
```

Claude Desktop 재시작 → "위고비 한국 허가 정보" 같은 질문에 KoRegX 도구 사용.

### claude.ai (Web)

Settings → Connectors → Add custom connector → URL: `https://koregx-xxxx.vercel.app/api/mcp`.

## Local Development

```bash
git clone https://github.com/drmoony/koregx.git
cd koregx
npm install
cp .env.example .env.local
# .env.local 채우기
npx prisma migrate dev
npm run dev -- --port 3300
```

API 키 발급:
- 식약처 OpenAPI: https://www.data.go.kr/data/15095677/openapi.do
- Clerk: https://dashboard.clerk.com
- Neon: https://console.neon.tech
- Gemini: https://ai.google.dev
```

- [ ] **Step 2: GitHub remote 등록 + push**

```bash
gh repo create koregx --public --source=. --description="Korea-anchored cross-region pharma regulatory web service + MCP" --push
```

또는 manual:

```bash
git remote add origin https://github.com/drmoony/koregx.git
git push -u origin main
```

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "M6-2: README — live demo URL + MCP connector instruction + local dev"
git push
```

---

## M7: Acceptance Test

### Task M7-1: Playwright e2e — Wegovy dossier query

**Files:**
- Create: `playwright.config.ts`, `e2e/acceptance-wegovy.spec.ts`

- [ ] **Step 1: Playwright 설치**

```bash
npm install -D @playwright/test
npx playwright install chromium
```

- [ ] **Step 2: playwright.config.ts**

```typescript
import { defineConfig } from '@playwright/test'

export default defineConfig({
  testDir: './e2e',
  use: {
    baseURL: process.env.E2E_BASE_URL ?? 'http://localhost:3300',
  },
  webServer: process.env.E2E_BASE_URL
    ? undefined
    : {
        command: 'npm run dev -- --port 3300',
        url: 'http://localhost:3300',
        reuseExistingServer: true,
        timeout: 120_000,
      },
})
```

- [ ] **Step 3: acceptance test**

`e2e/acceptance-wegovy.spec.ts`:

```typescript
import { test, expect } from '@playwright/test'

test.describe('Acceptance: Wegovy 한국 허가 dossier', () => {
  test('search → click → detail page shows MFDS approval', async ({ page }) => {
    await page.goto('/search?q=위고비')
    await expect(page.getByText(/위고비/i)).toBeVisible()
    await page.getByText(/위고비/i).first().click()
    await expect(page).toHaveURL(/\/drug\//)
    await expect(page.getByText('허가')).toBeVisible()
    await expect(page.getByText('KR')).toBeVisible()
    await expect(page.getByText('MFDS')).toBeVisible()
  })

  test('MCP chain_drug_dossier returns Wegovy', async ({ request }) => {
    const baseURL = process.env.E2E_BASE_URL ?? 'http://localhost:3300'
    const res = await request.post(`${baseURL}/api/mcp`, {
      data: {
        jsonrpc: '2.0',
        id: 1,
        method: 'tools/call',
        params: {
          name: 'chain_drug_dossier',
          arguments: { query: '위고비' },
        },
      },
    })
    expect(res.ok()).toBe(true)
    const body = await res.json()
    const text = body.result?.content?.[0]?.text ?? ''
    expect(text).toContain('위고비')
    expect(text).toContain('matched')
  })
})
```

- [ ] **Step 4: package.json script 추가**

```json
"e2e": "playwright test",
"e2e:prod": "E2E_BASE_URL=https://koregx-xxxx.vercel.app playwright test"
```

- [ ] **Step 5: 로컬 e2e 실행**

```bash
# 위고비 ingest 안 했으면 먼저
npm run ingest:mfds 위고비 20

# e2e 실행
npm run e2e
```

Expected: 2 passed.

- [ ] **Step 6: production e2e 실행**

```bash
E2E_BASE_URL=https://koregx-xxxx.vercel.app npm run e2e
```

Expected: 2 passed (production이 위고비 ingest 된 상태일 때).

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "M7-1: Playwright acceptance — Wegovy dossier (web + MCP)"
git push
```

### Task M7-2: Manual MCP connector check

**Files:** (없음, 수동 검증)

- [ ] **Step 1: Claude Desktop 또는 claude.ai에 connector 등록**

M6-2 README 가이드대로 등록.

- [ ] **Step 2: 자연어 query 테스트**

Claude Desktop / claude.ai 새 대화:

```
KoRegX로 위고비의 한국 허가 정보 조회해줘
```

Expected:
- Claude가 `koregx` MCP 도구 (`chain_drug_dossier` 또는 `search_drug`) 호출
- 응답에 "위고비", "MFDS", 허가 정보 포함
- 자연스러운 한국어 답변

- [ ] **Step 3: README에 스크린샷 추가 (옵션)**

스크린샷 캡처 → `docs/screenshots/connector-demo.png` 저장 → README에 임베드.

- [ ] **Step 4: Commit (스크린샷 추가 시)**

```bash
git add docs/screenshots/ README.md
git commit -m "M7-2: 수동 MCP connector 검증 + 데모 스크린샷"
git push
```

---

## Self-Review

### Spec coverage

| Spec § | Slice 1에서 다룬 task | 비고 |
|---|---|---|
| §1 motivation | M0-4 (랜딩에 KoRegX 메시징) | OK |
| §2 persona | (별도 task 없음, UX 톤만 적용) | Slice 1 minimal — Slice 2+에서 보강 |
| §3 roadmap | (Slice 1 자체가 v0.1의 일부) | OK |
| §4 architecture | M0~M5 전체 | OK |
| §5 data layer (b Hybrid) | M1 schema에 pgvector extension 활성화 | embedding pipeline은 Slice 4 |
| §6 data sources KR | M3 식약처만 | HIRA/복지부/KPIS는 Slice 2-3 |
| §7 schema | M1-1 schema.prisma | Decision/Reimbursement는 Slice 2 |
| §8 tool surface | M5 search_drug/get_drug/chain_drug_dossier | search_decision 등은 Slice 2+ |
| §9 tech stack | M0-1~M0-3 + M1 + M5 | OK |
| §10 branding | (이미 spec에서 KoRegX 확정) | OK |
| §11 acceptance criteria | M7-1 (1번 query) | 2-4번은 Slice 2-4 |
| §12 risks | M3-2 fixture 기반 test (PDF schema 변동 mitigation) | API rate limit / pgvector 비용은 후속 |
| §13 out of scope | (이 plan에 포함 안 함) | OK |
| §14 open questions | drug 정규화 = itemSeq 기반으로 정함 | cross-region matching은 v0.5 |
| App A estimate | Slice 1 ≈ 12-15 hours | OK |
| App B DoD | M1 schema, M3 ingest, M4 web UI, M5 MCP, M6 deploy, M7 acceptance | 5/14 완료 (나머지 후속 slice) |

### Placeholder scan

- "TBD" / "TODO": 없음
- "implement later": 없음
- "Similar to Task N": 없음 (모든 task에 코드 직접 표기)

### Type consistency

- `ParsedDrug` 타입 — M3-2 정의, M3-3 persist에서 동일 사용 ✓
- `Region` 타입 — M1-2 정의, M3-2/M5에서 import ✓
- `DrugWithApprovals` / `DrugWithFullDetail` — M1-2 정의, M4-1/M4-2/M5에서 사용 ✓
- Prisma model 필드명 — `itemSeq`, `productName`, `approvals`, `sourceDocs` 일관 ✓
- MCP tool `inputSchema` zod 객체 — M5-1 정의, M5-2에서 `parse` 호출 ✓
- `searchDrugTool.execute(args)` shape — M5-1 정의, M5-2 server call에서 일치 ✓

### Slice boundary 검증

Slice 1 종료 후 사용자가 받는 것:
- ✅ 식약처 의약품 검색 + 상세 web UI
- ✅ MCP server 3개 tool (search/get/chain)
- ✅ Vercel production deploy
- ✅ Claude Desktop connector 동작
- ✅ 1개 acceptance query ("Wegovy 한국 허가 timeline") 자연어 작동

Out of Slice 1:
- HIRA 약평위/암질심 (Slice 2)
- 복지부 고시 (Slice 3)
- pgvector embedding + LLM Q&A (Slice 4)
- KPIS + cron + monitoring (Slice 5)
- Cross-region (v0.5+)

### Estimated time

| Milestone | Steps | Claude-hours |
|---|---|---|
| M0 (scaffold) | 4 tasks | 2h |
| M1 (schema) | 2 tasks | 2h |
| M2 (source base) | 1 task | 1h |
| M3 (식약처) | 4 tasks | 4h |
| M4 (web UI) | 2 tasks | 2h |
| M5 (MCP) | 2 tasks | 3h |
| M6 (deploy) | 2 tasks | 1h |
| M7 (acceptance) | 2 tasks | 1h |
| **합계** | **19 tasks** | **~16h** |

User 결정 시간 (병목): P1-P5 prerequisite 30분 + plan 진행 중 결정 1-2h 추가.

---

## Future Slices (별도 plan 작성 예정)

- **Slice 2**: HIRA 약평위 + 암질심 PDF parser, Decision/Reimbursement schema, 결정사유 web view, MCP search_decision tool. Acceptance #2 ("삼성바이오에피스 약가협상 결정사유"), #3 ("2024 약평위 통과 GLP-1 RA").
- **Slice 3**: 복지부 고시 scraper, 고시 ↔ Reimbursement 자동 link. Acceptance #4 ("2024 보험약가 인하 고시").
- **Slice 4**: pgvector embedding pipeline (text-embedding-004), Decision rationale 의미검색, Gemini 2.5 Flash Q&A orchestrator + verify_citations.
- **Slice 5**: KPIS API 통합, Render cron worker, monitoring, error tracking. v0.1 production ready.
- **Slice 6 (= v0.2)**: 한국 헬스케어 관계법령 layer — 약사법·의료기기법·첨생법·국민건강보험법 등 8-12개 법령. 법제처 OpenAPI federation 또는 reimpl 결정. `search_law` / `get_law_text` / `get_annexes` / `chain_law_dispatch` tool 추가. Acceptance #8 ("약사법 § 38 변경 + 행정처분"), #9 ("약물군 적용 법령 dispatch"). ⚠️ 의료법은 본 scope 제외 — 별도 medical case law MCP 프로젝트 (`../medical-case-law-mcp-TODO.md`).

각 Slice마다 별도 plan 작성 + ship 후 next slice.
